Django>=4.2
python-dotenv
dj-database-url
gunicorn
whitenoise[brotli]
Pillow         # Required for ImageField support
groq          # Groq SDK for AI chatbot
requests       # Used by chatbot view to call external APIs
django-ipware       # Reliable client IP detection

# AI/ML dependencies for chatbot semantic filtering
# Note: These are large packages (~800MB total). Build may take 5-10 minutes on free tier.
# If build times out, consider upgrading to Starter plan or using alternative deployment strategy.
sentence-transformers>=2.2.2
torch               # Required by sentence-transformers

# PostgreSQL adapter for production
psycopg2-binary
