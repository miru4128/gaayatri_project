# Minimal requirements.txt without ML dependencies
# Use this for faster Render deployment if you don't need semantic filtering in the chatbot
# To use this: rename to requirements.txt before deploying

Django>=4.2
python-dotenv
dj-database-url
gunicorn
whitenoise[brotli]
Pillow         # Required for ImageField support
groq          # Groq SDK for AI chatbot
requests       # Used by chatbot view to call external APIs
django-ipware       # Reliable client IP detection
psycopg2-binary    # PostgreSQL adapter

# Note: This excludes sentence-transformers and torch to speed up deployment
# The chatbot will still work but without semantic similarity filtering
# To enable filtering, use the full requirements.txt instead
